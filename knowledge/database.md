# 数据库结构

## items 表

`items` 表保存商品基础信息。

字段说明：

- `id`：商品 ID，整数，主键。
- `name`：商品名称，字符串，不能为空。
- `price`：商品价格，浮点数，不能为空。

查询规则：

- 查询商品列表时使用 `items` 表。
- 用户提到“商品”“物品”“产品”时，通常对应 `items` 表。
- 用户提到“名称”“名字”时，通常对应 `items.name` 字段。
- 用户提到“价格”“售价”“金额”时，通常对应 `items.price` 字段。
