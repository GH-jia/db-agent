# 业务术语

## 商品

商品、物品、产品在当前系统中都表示 `items` 表中的记录。

## 商品名称

商品名称对应 `items.name` 字段。

## 商品价格

商品价格、售价、金额通常对应 `items.price` 字段。

## 价格范围

当用户询问“价格大于”“价格小于”“超过”“低于”时，应该使用 `items.price` 做条件过滤。

## 模糊搜索

当用户按名称搜索商品时，可以使用 `items.name ILIKE '%关键词%'` 进行模糊匹配。
